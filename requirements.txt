# =============================================================================
# requirements.txt — TFG Opciones Financieras
# =============================================================================
# Generado: 14/05/2026
# Entorno: Python 3.x (global)
# Uso:
#   pip install -r requirements.txt          (runtime)
#   pip install -r requirements-dev.txt      (runtime + tests)
# =============================================================================

# --- Interfaz web y API REST ---
streamlit==1.54.0
fastapi==0.136.1
uvicorn==0.47.0
python-dotenv==1.2.2

# --- Conectividad Interactive Brokers ---
ib-insync==0.9.86
nest-asyncio==1.6.0
eventkit==1.0.3          # dependencia transitiva requerida por ib-insync

# --- Motor financiero y análisis de datos ---
pandas==2.3.3
numpy==2.4.2

# --- Motor Black-Scholes (Día 3: heatmap + Greeks) ---
scipy==1.17.1

# --- Visualización (heatmap) ---
matplotlib==3.10.9
contourpy==1.3.3          # dependencia transitiva de matplotlib
cycler==0.12.1            # dependencia transitiva de matplotlib
fonttools==4.62.1         # dependencia transitiva de matplotlib
kiwisolver==1.5.0         # dependencia transitiva de matplotlib
pyparsing==3.3.2          # dependencia transitiva de matplotlib

# --- Dependencias transitivas de Streamlit ---
altair==6.0.0
attrs==25.4.0
blinker==1.9.0
cachetools==6.2.6
click==8.1.7
colorama==0.4.6
Jinja2==3.1.4
MarkupSafe==3.0.2
narwhals==2.16.0
packaging==26.0
pillow==12.1.1
protobuf==6.33.5
pyarrow==23.0.1
pydeck==0.9.1
python-dateutil==2.9.0.post0
pytz==2025.2
referencing==0.37.0
requests==2.32.5
rpds-py==0.30.0
six==1.17.0
tenacity==9.1.4
toml==0.10.2
tornado==6.5.4
typing_extensions==4.15.0
tzdata==2025.3
urllib3==2.6.3
watchdog==6.0.0
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
jsonschema==4.26.0
jsonschema-specifications==2025.9.1

# --- Ciberseguridad (Día 15: JWT) ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.28
bcrypt==5.0.0
